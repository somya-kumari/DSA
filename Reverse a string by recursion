#include <bits/stdc++.h> 
void Reverse(string &str,int i, int j){
	if(i>j){
		return;
	}
	swap(str[i],str[j]);
		i++;
		j--;
		Reverse(str,i,j);
	
}





string reverseString(string str)
{
	Reverse(str,0,str.length()-1);
	return str;
}
