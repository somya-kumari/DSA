class Solution {
public:
    void reverseString(vector<char>& s ) {
        // in place of start we take it first(f)
    
       int f= 0;
       int e = s.size()-1;
       while(f<e){
           swap(s[f++],s[e--]);
       }
    }
};
